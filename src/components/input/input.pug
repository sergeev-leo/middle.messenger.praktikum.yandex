mixin input(props)
  .input-wrapper(class=`${props.error ? "has-error" : ''}`)
    input(id=props.id name=props.id type=props.type required)
    label(for=props.id) #{props.label}
    span.error #{props.error}